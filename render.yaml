services:
  - type: web
    name: multiplayer
    env: node
    plan: free

    buildCommand: npm install && npm run build
    startCommand: npm start

    envVars:
      - key: HOST
        value: 0.0.0.0

      - key: DATABASE_URL
        sync: false

      - key: JWT_SECRET
        sync: false